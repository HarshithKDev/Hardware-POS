import fs from 'fs';
import { openDB } from 'idb';

async function run() {
  // Can't run idb easily from node without a browser environment.
  console.log("To simulate, I can just write a quick button in OwnerStats to insert a fake failed transaction, or just verify the code.")
}
run();
